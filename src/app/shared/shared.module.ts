import { NgModule } 		from '@angular/core';

// Directives
// import { ValidMessage } 	from './directives/validMessage.directive';

// Service


@NgModule({
    declarations: [
        // ValidMessage
    ],
    exports: [
        // ValidMessage
    ]
})

export class SharedModule{}